server:
  port: 12330

spring:
  application:
    name: user-service
  cloud:
    config:
      profile: dev
      label: master
      uri: http://localhost:7001


eureka:
  client:
    service-url:
      defaultZone: http://localhost:1111/eureka/
